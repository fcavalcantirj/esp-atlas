{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://esp.wiki/schema/board.schema.json",
  "title": "ESP Development Board",
  "description": "A dev board built on a module (or a bare SoC). Inherits radio/CPU specs through `module` -> `soc`. State exactly one of `module` or `soc`. Every added spec carries a source.",
  "type": "object",
  "required": ["id", "type", "brand", "name", "sources"],
  "additionalProperties": false,
  "anyOf": [
    { "required": ["module"] },
    { "required": ["soc"] }
  ],
  "properties": {
    "id": { "type": "string", "pattern": "^[a-z0-9-]+$" },
    "type": { "const": "board" },
    "brand": { "type": "string", "description": "vendor/maker; matches data/boards/<brand>/ folder" },
    "name": { "type": "string" },
    "aka": { "type": "array", "items": { "type": "string" } },
    "module": { "type": "string", "description": "id of the module it uses (must exist in data/modules/)" },
    "soc": { "type": "string", "description": "id of the SoC, when the board uses a bare chip (must exist in data/socs/)" },
    "flash_mb": { "type": "number" },
    "psram_mb": { "type": "number" },
    "form_factor": { "type": "string" },
    "price_tier": {
      "enum": ["cheap", "medium", "expensive"],
      "description": "APPROXIMATE, EDITORIAL street-price bucket — NOT a datasheet-verified spec, exempt from the sources rule. Optional; omit rather than guess. Used only by the wizard's budget filter, never rendered alongside hard specs."
    },
    "dimensions_mm": { "type": "array", "items": { "type": "number" }, "minItems": 2, "maxItems": 3 },
    "usb": {
      "type": "object", "additionalProperties": false,
      "properties": {
        "connector": { "enum": ["usb-c", "micro-usb", "mini-usb", "none"] },
        "bridge": { "type": "string", "description": "USB-UART bridge chip (e.g. cp2102, ch340) or 'native' if the SoC's own USB is used" }
      }
    },
    "power": {
      "type": "object", "additionalProperties": false,
      "properties": {
        "battery_connector": { "type": "boolean" },
        "charging": { "type": "boolean" }
      }
    },
    "display": { "type": "string", "description": "e.g. '1.9in 170x320 IPS' — only if the board has an onboard display" },
    "extras": { "type": "array", "items": { "type": "string" }, "description": "onboard peripherals: sd-card, imu, mic, rgb-led, gps, etc." },
    "io": {
      "type": "object", "additionalProperties": false,
      "description": "Usable GPIO exposure and external-load power delivery — board-level, vendor-sourced (SPEC-io-power.md §2). Every field cite-or-omit; absence is neutral, never inventive.",
      "properties": {
        "gpio_exposed": { "type": "number", "description": "count of GPIO pads routed to a header/pin, as printed by the vendor (e.g. 'IO x6')" },
        "gpio_free": { "type": "number", "description": "gpio_exposed minus the SoC's exposed reserved_pins (strapping/input-only/usb-flash-tied) and any pins hard-consumed onboard; DERIVED unless the vendor states it directly — record the subtraction in notes" },
        "gpio_pins": { "type": "array", "items": { "type": "number" }, "description": "the GPIO numbers broken out to a user-accessible header (the raw exposed set, before subtracting reserved). Cited to the same pinout source as gpio_exposed/gpio_free; cite-or-omit." },
        "input_only": { "type": "number", "description": "count of exposed pads that are input-only (no output capability)" },
        "power_out": {
          "type": "object", "additionalProperties": false,
          "properties": {
            "rail_v": { "type": "array", "items": { "type": "number" } },
            "rail_ma_max": { "type": "number" },
            "high_side_switch": { "type": "boolean" }
          }
        }
      }
    },
    "download_mode": {
      "type": "object", "additionalProperties": false,
      "description": "How to enter Firmware Download mode for flashing (SPEC-first-flash.md P0). Cite-or-omit to the board's own user guide -- hardware steps must be verifiable, never guessed.",
      "required": ["mode"],
      "properties": {
        "mode": { "enum": ["auto", "manual"], "description": "'auto' -- USB-serial bridge toggles EN/IO0 via DTR/RTS, just click flash. 'manual' -- the exact button sequence, given in `steps`." },
        "steps": { "type": "string", "description": "the exact manual sequence, e.g. 'hold BOOT/IO0, tap RESET/EN, release BOOT'; required when mode is 'manual'" },
        "note": { "type": "string", "description": "board-specific gotcha, e.g. which of multiple USB ports to use" }
      },
      "if": { "properties": { "mode": { "const": "manual" } } },
      "then": { "required": ["steps"] }
    },
    "usb_serial": {
      "enum": [
        "native-usb-serial-jtag",
        "cp2102",
        "cp2102n",
        "ch340",
        "ch343",
        "ch9102",
        "usb-uart-bridge-unspecified",
        "other"
      ],
      "description": "USB-UART bridge chip actually used to flash -- drives which driver the troubleshooter recommends (SPEC-first-flash.md P0)."
    },
    "first_flash_notes": {
      "type": "array", "items": { "type": "string" },
      "description": "Board-specific first-flash gotchas the connect troubleshooter shows (SPEC-first-flash.md P0), e.g. a current-measurement header whose jumper must be fitted or the module is unpowered. Cite-or-omit to the board's own user guide -- hardware facts must be verifiable, never guessed."
    },
    "getting_started": {
      "type": "string",
      "format": "uri",
      "description": "Link to the board's OFFICIAL getting-started / user-guide page (SPEC-data-completion.md board field-to-add). Cite-or-omit to the vendor doc; the URL must resolve on the vendor's own domain."
    },
    "notes": { "type": "array", "items": { "type": "string" } },
    "sources": {
      "type": "array", "minItems": 1,
      "items": {
        "type": "object", "required": ["field", "url", "verified"], "additionalProperties": false,
        "properties": {
          "field": { "type": "string" },
          "url": { "type": "string", "format": "uri" },
          "verified": { "type": "string", "format": "date" }
        }
      }
    }
  }
}
