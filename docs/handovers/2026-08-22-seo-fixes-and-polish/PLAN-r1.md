---
round: 1
builder_session: fresh session 2026-08-22 (ultracode effort) — read only the handover dir, re-verified every [REAL] claim against origin/main, gh, production and the live API before planning
---

# Plan r1 — SEO fix series (F4–F6) + UI polish on esp-atlas.com

## Blocking constraints, restated

1. `main` is PR-only. Required checks `schema` + `tests`; `strict: true` means the branch must contain the current `main` tip before merging (rebase, never merge-commit); `enforce_admins: true` means no bypass even as owner. Merge = `gh auth switch --user fcavalcantirj` → `gh pr merge N -R fcavalcantirj/esp-atlas --squash --delete-branch` → `gh auth switch --user fcavalcanti-onvida`. Never `git push origin main`.
2. The Vercel glue is untouchable: `apps/web/api/index.py`, `apps/web/vercel.json`, `apps/web/requirements.txt`, the `vercel-build` script in `apps/web/package.json`, `eslint.config.mjs`, `next.config.ts`. No Tailwind, no shadcn, no `@vercel/analytics`.
3. Thin client: anything that filters, counts, ranks or derives lives in `esp_atlas_core` + FastAPI, with tests; Next only fetches and renders. Hence F5 starts with the core `brand` filter, and the home "Browse by chip" counts come from `/facets.soc_ref` — which counts the SoC's own record and its modules — so the label is "N parts on this chip", never "N boards".
4. `data/` is source-or-omit: no record edit without an official citation, and whether to edit at all is Felipe's. `price_tier` is editorial — never rendered as a spec, never in JSON-LD, never on the OG image, no `offers`.
5. No secrets in the repo. SA / PSI keys live only in a scratchpad and are revoked/deleted after use. The GA measurement ID is public and fine in code.
6. GA4 event and parameter names are contracts: add, never rename. New `origin` values `"browse"` / `"brand"` are fine (the `origin` dimension is registered); any new *parameter* would need Admin-API registration — this plan adds none.
7. Polish is visual only: no change to what pages contain, no data logic, no new routes in the polish PR; palette and component tree stay; the work is applying the restyle's own tokens/system consistently, with before/after screenshots where a judgement call is involved.

## Handover discrepancies

Re-verified 2026-08-22 (late) / 2026-08-23 UTC. Checked: `git ls-remote origin main`, `gh pr list --state all`, branch protection API, production curls with a Googlebot UA (`/`, `/compare?ids=…`, `/parts/adafruit-feather-esp32-s3`, `/parts/nope`, `/llms.txt`, `/sitemap.xml`, `/og-default.png`, `www.`), `/api/facets`, `/api/search`, `/api/parts`, every cited file/line in `apps/web`, `apps/core`, `apps/api`, `apps/cli`, and local tooling.

**Mismatches**
- `main` = `8b683ee` (PR #9, the handover's own docs PR, merged after the handover was written), not `5286632`. PRs #1–#9 all MERGED with the stated squash SHAs; **0 open PRs**, so there is no CI state to report.
- `/parts/esp32-s3` has **34** related (33 boards + 1 module); "33" is the board page's count (`/parts/adafruit-feather-esp32-s3`: 32 boards + 1 module).
- Corpus is **94 parts → sitemap is 95 URLs** (94 + `/`). F4's crawl target is therefore "95 sitemap URLs at depth ≤ 2" (the handover's "96" predates F3); F5's sitemap target is 95 + 1 (`/brands`) + 11 = **107**, not 108.
- CSS anchors drifted by a few lines but the rules are as described: `.part-aside` sticky block is `app/globals.css:1265-1270` (inside the `@media (min-width: 1024px)` opened at 1259); `.compare-layout { grid-template-columns: 1fr }` is the property at **:1566**; `.compare-picker` sticky block :1576-1581 (`max-height` 1579, `overflow-y` 1580 ✓); `.chip-chain` :1319 ✓; `.breadcrumb` :1239 ✓; `.segmented` :1626; `@media (max-width: 599px)` :1787, and `--page-x` is indeed only defined at :37 ✓.
- "one `application/ld+json` block" is correct — but `grep -c application/ld+json` reports 2 per page because the RSC flight payload repeats the string; the exact tag count (`<script type="application/ld+json"`) is 1 on `/` and 1 on part pages. Part graph = Organization + WebSite + TechArticle (with nested `about: Product{brand: Brand}`) + BreadcrumbList.

**Clean** (as written): canonical `https://esp-atlas.com` on `/`, `twitter:card summary_large_image`, `og:image /og-default.png` 200 / 27 938 B; `/compare?ids=` → `noindex, follow` + canonical `/compare` + own `og:url`; `/llms.txt` 200 `text/plain`; sitemap 95 `<loc>` / 95 `<lastmod>` / `/compare` absent; `/parts/nope` 404 + `noindex`; `www.` → 307 (still Felipe's dashboard item); branch protection `{checks:[schema,tests], strict:true, enforce_admins:true, reviews:0}`; `gh` keyring (active `fcavalcanti-onvida`, `fcavalcantirj` present); `lib/api-server.ts` 58 lines with private `serverFetch`, `TIMEOUT_MS = 3000`, `REVALIDATE_SECONDS = 3600`; `search.py` lines 17-19 / 37-40 / 56-94 / 68 / 71 / 110; `main.py:71-114`; CLI `main.py:30-59`; tests at `test_search.py:140`, `test_app.py:331`, `test_cli.py:208`; `ResultOrigin` at `analytics.ts:49`; `SearchFilters` `api.ts:72-85`, `searchParts` 139-141; `sitemap.ts:39`; footer Explore `SiteFooter.tsx:91-105`; root `llms.txt:19`; brand counts (espressif 30 = 17 `vendor:` + 13 `brand:`, adafruit 11, m5stack 11, lilygo 9, lolin 6, dfrobot 6, sparkfun 5, heltec 5, unexpected-maker 4, soldered 4, seeed 3); Next `16.3.2`; Chrome at the stated path; ports 3000/8010 free, 8000 held by Docker; `validate.yml` jobs `schema`, `tests` (+ `sources-live`, continue-on-error).

**New facts the handover lacks (matter for the plan)**
- `/api/search?brand=adafruit` **today returns 200 with all 94 parts** — FastAPI ignores unknown query params. F5's verification must assert the count (11), never a status code.
- `data/brands/` exists but is **empty**; there is no brand schema or record. Brand display names can only be the slug, which the site already prints verbatim (`by adafruit`).
- Live `soc_ref` facet: s3 35, esp32 27, c3 9, c6 8, s2 7, h2 3, c2/c5/c61/h4/p4 1 each. Every SoC record has `soc_ref = <own id>`, so each count includes the chip itself.
- `ChipChain.tsx:22` wraps every parent in a second `<span className="chip-chain">` — an inner `display: flex; flex-wrap: wrap` box — which is exactly what lets the arrow wrap alone. The fix is one CSS rule, no markup change.
- `.footer-title`'s own comment says "one label style site-wide", yet `.aside-card h2` (:1455), `.spec-group h3` (:1383), `.related-group h3` (:1516) and `.compare-group > summary` (:1656) still carry the pre-restyle serif uppercase labels — the most likely reading of "look&feel not properly applied on ALL pages". Proposed as a gated sweep finding (P2-f), not assumed.
- `apps/web/README.md:73` documents `origin (wizard|search|related|compare)` — update with `browse|brand` in F4/F5.
- Repo has `allow_auto_merge: true` → `gh pr merge --auto --squash --delete-branch` is the "auto-merge when green" mechanism. `lighthouse` and ImageMagick are not installed (→ `npx lighthouse`, Pillow in the scratchpad venv for pixel diffs).

## Acceptance checklist — point-by-point

1. Seven constraints restated in my own words, first section → done above.
2. `[REAL]` claims re-verified; `main` head, PR list, open PRs/CI, F1–F3 production facts and CSS line numbers each addressed, mismatches listed → "Handover discrepancies".
3. Polish sweep matrix defined before any fix → Step P1 (8 routes × 4 widths × 2 themes + 4 state shots = 68 files, each named by path; findings file tags CSS rule + line).
4. Each known issue has a named fix keeping the restyle's system → Step P2 table (a)–(g); verification = same matrix re-shot + CLS < 0.1 → Step P4.
5. F4 → Step F4 (`revalidate = 300`, `fetchFacets()` never throws, 11 "N parts on this chip" links, SoC `<h2>` only when n > 0, `ItemList`, `"browse"` origin, brand browse deferred to F5, crawl 0 orphans / depth ≤ 2 over the 95 sitemap URLs).
6. F5 → Step F5 (core `_EXACT_FILTERS` + `parts.vendor_or_brand = ?`, API param, CLI flag, tests in the three suites mirroring `soc`, then `/brands` + `/brands/[brand]` SSR with `generateMetadata`/canonical/JSON-LD, sitemap, footer link to `/brands`; verification pytest + count 11 + sitemap 107).
7. F6 → Step F6 (`opengraph-image.tsx`, `ImageResponse`, `params` awaited, name/brand/SoC/≤3 chips, no price, explicit `images` removed, 5-part spot check).
8. Order and gating → "Sequence & gating" (polish → F4 → F5 → F6 → F7 only on yes → docs; auto-merge when green + current; prod verified after each).
9. `## Concerns` and `## Questions for the author` close the plan; the handover's three open questions are answered as Q1–Q3.

## The plan

### Environment (once per working session; kill previous processes first — house rule)

`S=/private/tmp/claude-501/-Users-fcavalcanti-dev-esp-atlas/f7cc66c2-bce3-4f6a-b450-ffae4570c091/scratchpad`, then the handover's recipe verbatim (venv with `pytest jsonschema pyyaml httpx fastapi click uvicorn`, plus `websocket-client pillow` for measurement/diffs; pytest over the three suites; uvicorn on **8010**; `npx next typegen && npx tsc --noEmit && npm run lint && NEXT_PUBLIC_API_URL=http://localhost:8010 npm run build && NEXT_PUBLIC_API_URL=http://localhost:8010 npx next start -p 3000`). Absolute paths in every Bash call (the `cd` trap). Screenshots with the handover's Chrome recipe, never `--user-data-dir`.

### Step P — polish PR, branch `polish/editorial-consistency` (from fresh `origin/main`)

**P1 — Sweep matrix (shot BEFORE any CSS edit, into `$S/shots/before/`)**

| Routes (8) | Widths (4) | Themes (2) |
|---|---|---|
| `/` · `/compare` · `/compare?ids=esp32-s3,esp32-c3,esp32` · `/parts/adafruit-feather-esp32-s3` (board, 33 related) · `/parts/esp32-s3` (SoC, 34 related) · `/parts/esp32-c5` (SoC, 0 related) · `/parts/esp32-c6-wroom-1` (module) · `/parts/nope` (404) | 390 and 768 via the iframe wrapper (`--window-size=600,2300` / `900,2300`), 1024 and 1440 direct (`--window-size=W,1800`) | light · dark (`--force-dark-mode`, asserted once per width with `--dump-dom \| grep 'data-theme="dark"'`) |

File name `<slug>-<width>-<theme>.png` (slugs `home`, `compare-empty`, `compare-3`, `board`, `soc-s3`, `soc-c5`, `module`, `404`) = 64 shots. States (+4): a second build with `NEXT_PUBLIC_API_URL=http://10.255.255.1:9 API_INTERNAL_URL=http://10.255.255.1:9` makes the server fetch time out and the client fetch hang → `PartDetailClient` loading skeleton persists (`board-390/1440-light-loading.png`); `…=http://127.0.0.1:9` → immediate refusal → error state (`…-error.png`). `app/error.tsx` needs a thrown render and is **not** reproducible without a code change — skipped, said so.

Measurement (`$S/measure.py`, CDP over `--remote-debugging-port` + `Emulation.setDeviceMetricsOverride`, per route × width): `documentElement.scrollWidth` vs `innerWidth`; for `.part-aside`, `.compare-picker`: `scrollHeight > clientHeight` (inner scroll); `.segmented` width vs its parent; each `.chip-chain-arrow` line box vs its following `.chip-chain-item` (same `top` ⇒ not stranded); `.site-header-inner`/`main`/`.site-footer-inner` computed `padding-left`; smallest computed `font-size` per route. Output `$S/findings.md`: one row per finding → route, width, symptom, **CSS selector + `globals.css` line**, planned fix. This file is pasted into the PR body.

**P2 — Fixes** (`apps/web/app/globals.css` unless noted; every rule cited by current line)

| # | Issue | Fix | System kept |
|---|---|---|---|
| a | Inner scrollbars: `.part-aside` :1265-1270 and `.compare-picker` :1576-1581 (`position: sticky; top; max-height: calc(100dvh…); overflow-y: auto`) | Delete the four sticky/scroll declarations in both blocks → the aside/picker flow with the page (always static; Q3). `.home-sidebar` keeps its sticky (no `max-height`, short, no finding). | Same boxes, same grid; only scroll behaviour changes |
| b | Phone `/compare` clipped: `.compare-layout { grid-template-columns: 1fr }` :1566 lets the table's min-content widen the column | `grid-template-columns: minmax(0, 1fr)` + new `.compare-layout > * { min-width: 0; }` so `.compare-table-wrap { overflow-x: auto }` (:1721) finally scrolls. CLS: `app/compare/page.tsx:37` Suspense fallback becomes a `div.compare-layout[aria-busy]` holding a placeholder picker (summary line, disabled search input, segmented shell) and a `section.compare-placeholder` with a `min-height` taken from the measured loaded layout at 390/1024/1440, so `footer.site-footer` starts where it ends up. The later growth of the picker when `/api/parts` arrives is the residual → Concern HIGH + Q4. | Placeholder reuses `.compare-picker`, `.segmented`, input styles — no new visual language |
| c | Chip chain on phones: inner `span.chip-chain` (ChipChain.tsx:22) wraps the arrow alone; long current chip wraps its name and parks the badge at the far edge | Default **shrink-inline** (CSS only): `.chip-chain > .chip-chain { display: inline-flex; flex-wrap: nowrap; }` (arrow + chip travel together) and `.chip-chain-item { max-width: 100%; flex-wrap: wrap; row-gap: 0.15rem; }` (name wraps inside its chip, badge trails the last word). Alternative **stack** shot too: under 600 px `.chip-chain { flex-direction: column; align-items: flex-start }` with the arrow rotated 90°. Both before/after pairs go to Felipe (hard rule); markup stays unless he picks *stack* and a `chip-chain-step` hook is needed (one-line edit at ChipChain.tsx:22). | Chips, badge, arrow unchanged |
| d | Footer gutter 18.75 px vs 15 px below 600 px | In `@media (max-width: 599px)` (:1787) add `:root { --page-x: 1rem; }`; replace the literal `1rem` in that block's `.container` / `.site-header-inner` overrides with `var(--page-x)` (same px, one source). | Single gutter token as the restyle intended |
| e | `.segmented` stretched to picker width (column flex parent `.compare-picker-body` :1619) | `.compare-picker-body > .segmented { align-self: flex-start; }` | — |
| f | Sweep findings, incl. the already-visible label drift: `.aside-card h2` :1455, `.spec-group h3` :1383, `.related-group h3` :1516, `.compare-group > summary` :1656 (serif, uppercase, ad-hoc 0.78–0.9 rem) vs the restyle's mono overline (`.panel-title` :739 / `.footer-title` :409: `--font-data`, `--t-micro`, `--track-label`) | One shared overline rule applied to those four selectors, plus whatever `findings.md` adds — **each with a before/after pair; visible ⇒ Felipe's nod before merge.** Ad-hoc rem sizes within ~1 px of the nearest token are left alone (4g). | Reuses the restyle's existing label style |
| g | No pixel change where nothing was wrong | Pixel diff (Pillow) of before/after at 1024/1440 for `/` and `/parts/nope` (no fix applies) must be 0; for the other routes only the regions named in `findings.md` may differ — stated in the PR. | — |

Also: breadcrumb wrapping at 390 (`.breadcrumb` :1239, the part name drops to its own line) is recorded in `findings.md` as a design read with a screenshot; default = leave.

**P3 — Gate**: `npx next typegen && npx tsc --noEmit && npm run lint && npm run build` (absolute paths).

**P4 — Verify**: full matrix re-shot to `$S/shots/after/` + `measure.py` again → 0 horizontal overflow at 390/768; `.part-aside`/`.compare-picker` never `scrollHeight > clientHeight`; header/main/footer padding equal below 600 px; no arrow without its chip on the same line; `.segmented` width = content. Commit, push, `gh auth switch --user fcavalcantirj`, `gh pr create` with `findings.md` + before/after paths in the body (screenshot hosting → Q5), `gh pr merge --auto --squash --delete-branch`, switch back. After the Vercel deploy: prod shots 390/1440 for `/compare` and the S3 board page; CLS via `npx lighthouse https://esp-atlas.com/compare --only-categories=performance --form-factor=mobile` and `--preset=desktop`, JSON out (no key needed); PSI API as a cross-check if the anonymous quota allows. Target < 0.1 on both.

### Step F4 — `seo/f4-browse-by-chip`

- `lib/api-server.ts`: `export function fetchFacets(): Promise<ServerFetchResult<Facets>>` → `serverFetch<Facets>("/facets")` (never throws, 1 h cache, 3 s bound).
- `app/page.tsx`: `export const revalidate = 300`; async; `Promise.all([fetchFacets(), fetchAllParts()])`; join `facets.soc_ref` (order = facets order, count desc, sorted by core) with `parts.filter(p => p.type === "soc")` for names; render `<BrowseByChip items=…/>` only when facets are `ok` and the join yields ≥ 1 item (build-time render omits it; ISR fills it within 5 min of the first request — REVIEW-r1 answer 11). Placement: full-width `section.home-browse` after `<HomeView />` (Q6).
- `components/BrowseByChip.tsx` (`"use client"`, for `track`): overline `BROWSE BY CHIP` (existing `.panel-title` style), `ul.chip-row` of `<Link href="/parts/<soc>">` styled with the existing `.chip`, text `ESP32-S3 · 35 parts on this chip`, `onClick → track("result_click", { part_id, part_type: "soc", origin: "browse", position })`.
- `lib/analytics.ts:49`: `ResultOrigin` + `"browse"`; `apps/web/README.md:73` updated.
- SoC hub: `components/part/PartDetailView.tsx` — when `part.type === "soc" && part.related.length > 0`, after `<SpecGroups>`: `<section aria-labelledby="on-chip"><h2 id="on-chip">Boards and modules on {part.name} ({part.related.length})</h2><ul className="results-list">{related.map(… <PartResultCard origin="browse" position=…/>)}</ul></section>` (core already orders `related` by type, name). Aside "Related parts" on SoC pages → Q7 (default: unchanged).
- `lib/structured-data.ts` `partGraph`: same condition adds `{ "@type": "ItemList", "@id": `${url}#parts`, name: "Boards and modules on X", numberOfItems, itemListElement: [{ "@type": "ListItem", position, url, name }] }` to the `@graph`.
- CSS: `.home-browse` spacing only (reuses `.panel-title`, `.chip-row`, `.chip`).
- Verify: gate (P3); local `curl -A Googlebot http://localhost:3000/` contains the 11 `/parts/<soc>` links (after the first ISR regeneration, i.e. second request ≥ 5 min later — locally `revalidate` behaves the same with `next start`); `/parts/esp32-s3` HTML has the `<h2>` and an `ItemList` with 34 items; `/parts/esp32-c5` has neither. Prod after merge: same curls; `$S/crawl.py` (BFS from `/` with a Googlebot UA over internal `<a href>` only, depth map) → all 95 sitemap URLs reachable, max depth 2, 0 orphans, 0 unreachable. Note: `/compare` stays out of the sitemap; `/parts/nope` is not a URL.

### Step F5 — `seo/f5-brand-filter-and-hubs` (one PR; commits ordered core → API → CLI → web)

- Core `apps/core/src/esp_atlas_core/search.py`: `:18` `_EXACT_FILTERS = {"type", "form", "soc", "module", "brand"}`; in `_build_where` after the `module` clause (:72-74): `if "brand" in filters: clauses.append("parts.vendor_or_brand = ?"); params.append(filters["brand"])` — exact match like `soc` (Q8). Tests `apps/core/tests/test_search.py` after `:160`: `test_search_brand_filter_returns_only_parts_of_that_brand` (`{"brand": "adafruit"}` → non-empty, every `vendor_or_brand == "adafruit"`, `adafruit-feather-esp32-s3` in ids), `test_search_brand_filter_combines_with_type` (`brand=espressif, type=soc` → every row `soc`/`espressif`, `esp32-c6` in ids), `test_search_unknown_brand_returns_empty`.
- API `apps/api/src/esp_atlas_api/main.py`: `brand: Optional[str] = None` after `module` (:83) and `if brand is not None: filters["brand"] = brand`; tests `apps/api/tests/test_app.py` after `:346`: `test_search_brand_filter`, `test_search_brand_filter_combined_with_type` mirroring `:331-346`.
- CLI `apps/cli/src/esp_atlas_cli/main.py`: `@click.option("--brand", default=None, help="Only parts from this vendor/brand slug, e.g. adafruit")`, parameter added to `search(...)`, `if brand: filters["brand"] = brand`; test `apps/cli/tests/test_cli.py` after `:213`: `test_search_command_brand_filter` (`["search", "", "--brand", "adafruit", "--type", "board"]` → `adafruit-feather-esp32-s3` in output, `xiao-esp32c6` not). `_print_records` unchanged (ids carry the brand).
- Docs: root `llms.txt:19` → `GET /api/search?q=&soc=&brand=&type=`; `apps/web/README.md` (`origin` list, `/brands` in Layout if it lists routes).
- Web: `lib/api.ts` `SearchFilters.brand?: string`; `lib/api-server.ts` `fetchPartsByBrand(brand)` → `serverFetch<{ results: PartRecord[] }>(`/search?brand=${encodeURIComponent(brand)}`)`; `app/brands/page.tsx` (server, per-request like part pages): `fetchFacets()` → list of `{ value, count }` as `<Link href="/brands/<value>">` "N parts", `metadata` title/description/`alternates.canonical: "/brands"` + restated `openGraph`/`twitter` (Next merge rule), JSON-LD `CollectionPage` + `ItemList`; on facets error → `robots: { index: false }` + empty-state body (never a 500). `app/brands/[brand]/page.tsx`: `generateMetadata` + page both call `fetchPartsByBrand`; `results.length === 0` → `notFound()` (404 + noindex, like parts); title `"{brand} — ESP32 boards and modules"` (wording Q9), canonical `/brands/<brand>`; body: breadcrumb Home › Brands › {brand}, `<h1>`, lead "{n} parts in esp-atlas from {brand}" (n = length of what core returned — a render, not a derivation), groups by type in RelatedParts' order, each a `ul.results-list` of `PartResultCard origin="brand"`; JSON-LD `CollectionPage` + `ItemList` + 3-level `BreadcrumbList` (new page, so no "visible change" issue). Route slug = `vendor_or_brand` verbatim (`unexpected-maker`). `ResultOrigin` + `"brand"`.
- `app/sitemap.ts`: from the already-fetched `parts`, distinct `vendor_or_brand` → `/brands/<slug>` (`lastModified` = max `lastVerified` of its parts, `weekly`, 0.6) + `/brands` (`lastModified` = newest, `weekly`, 0.6); pushed into the `[...staticRoutes, …]` array at `:39`.
- `components/SiteFooter.tsx` Explore (`:91-105`): one `<li><Link href="/brands">Brands</Link></li>` — not 11 links.
- Home "Browse by brand" (F5-gated per REVIEW-r1): same `BrowseByChip` component generalised (`items`, `heading`) fed by `facets.vendor_or_brand` → `/brands/<slug>` links, `origin: "brand"` (Q10).
- Verify: three pytest suites green locally (`$S/venv`) and in CI; `curl /api/search?brand=adafruit` → exactly 11 results, `?brand=nope` → 0, `?brand=adafruit&type=module` → 0; `$S/venv/bin/esp-atlas --db $S/esp-atlas-dev.db search "" --brand adafruit` lists 11; local build → `/brands` 200 with 11 links + canonical + 1 JSON-LD tag; `/brands/unexpected-maker` 200 (4 parts) + canonical + `og:url`; `/brands/nope` 404 + `noindex`; sitemap = **107** `<loc>`. Prod after merge: same curls + `gh run` green.

### Step F6 — `seo/f6-part-og-image`

- New `app/parts/[id]/opengraph-image.tsx`: `import { ImageResponse } from "next/og"`; `export const size = { width: 1200, height: 630 }`, `export const contentType = "image/png"`, `export const alt = "…"`; `export default async function Image({ params }: { params: Promise<{ id: string }> })` → `const { id } = await params; const r = await fetchPartDetail(id)`. On `ok`: flex layout with the site wordmark (`SITE_NAME`), `part.name`, `typeLabel(type)` + `vendor_or_brand`, SoC line (`part.chain.soc?.name`, or the part itself when it is a SoC), up to three `specChips(part).slice(0, 3)` labels — **never `price_tier`**; colours from the light tokens (`#ffffff` / `#16181d` / `#0b6e8c` / `#e3e6ea`); `next/og`'s bundled font for r1 (Q11). On `not_found` / `error`: a generic site card (wordmark + tagline) so the route never 500s. Edge-vs-Node: default Node runtime (no `runtime` export).
- `app/parts/[id]/page.tsx:34-35`: drop `images: [OG_IMAGE]` and `images: [OG_IMAGE.url]` (keep `siteName`, `url`, `title`, `description`, `card`) so the segment file supplies `og:image` / `twitter:image`; drop the now-unused `OG_IMAGE` import. `lib/structured-data.ts:84` `TechArticle.image` → `${url}/opengraph-image` (Q12).
- Verify: gate; `curl -o $S/og-s3.png http://localhost:3000/parts/esp32-s3/opengraph-image` → 200 `image/png`, `sips -g pixelWidth -g pixelHeight` = 1200×630, size noted (target < 200 KB); `--dump-dom` of the part page shows `og:image` and `twitter:image` pointing at `/parts/<id>/opengraph-image?…`; visual spot-check (Read the PNGs) of 5: `adafruit-feather-esp32-s3` (long name), `esp32-s3` (SoC), `esp32-c6-wroom-1` (module), `esp32-c5` (chip-only), one `unexpected-maker` board; `/parts/nope/opengraph-image` → generic card, 200. Prod after merge: same 5 + `/` and `/compare` still on `/og-default.png`.

### Step D — `docs/seo-audit-status-2026-08`

`docs/seo-audit/2026-08-22.md` §13 table A: rows PR-F4/F5/F6 → "merged (#N, `sha`)", the "web fixes outside the PR series" line → compare clipping + CLS "merged (#N)" with the measured CLS, iOS floor / `TIMEOUT_MS` unchanged (Felipe's call), F7 per his answer. Nothing else in the audit is rewritten.

### Sequence & gating

polish → F4 → F5 → F6 → (F7 only on an explicit yes) → docs. Each PR: `git fetch origin && git checkout -b <branch> origin/main`; one concern; gate; commit (one-line conventional subject, trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`); push; `gh auth switch --user fcavalcantirj` → `gh pr create` (body: what / why / verification run / screenshot paths; `🤖 Generated with [Claude Code](https://claude.com/claude-code)`) → `gh pr merge --auto --squash --delete-branch` → `gh auth switch --user fcavalcanti-onvida`. If `main` moved meanwhile: `git rebase origin/main` + force-push the branch (strict rule). Wait for the Vercel production deploy, run the step's prod verification, only then branch the next step. F4 and F6 both edit `app/parts/[id]/page.tsx` — strictly sequential, never parallel branches. No `data/`, `next.config.ts`, robots or `.env` edits anywhere in this series.

## Concerns

- [HIGH] **CLS < 0.1 on `/compare` mobile is unlikely with the agreed skeleton alone.** The fallback → layout switch is fixable by reserving the footprint, but the dominant phone shift is the picker growing by roughly 600 px when `/api/parts` arrives after hydration (boards collapsed, modules + SoCs open) while the empty state sits in the viewport. Robust fix = SSR the picker list: `app/compare/page.tsx` becomes async with `fetchAllParts()` + `export const revalidate = 300` (same pattern REVIEW-r1 approved for `/`) and passes `initialParts` to `CompareView`, client fetch kept only as fallback; `noindex,follow`, canonical and the `?ids=` client logic are untouched — but it brushes the "client-rendered" decision → Q4. If declined, I reserve a measured `min-height` on the picker body while `parts === null` (a magic number tied to the 94-part corpus) and report the measured CLS as is.
- [MEDIUM] P2-f (label overline alignment) is the likeliest answer to "not properly applied on ALL pages" and is visible; gated on Felipe's nod. If he declines, the PR still ships a–e and I state exactly which inconsistencies remain.
- [MEDIUM] F4 ISR: `revalidate = 300` on the page plus `serverFetch`'s 1 h fetch cache means data changes reach `/` only on the next deploy (which resets the cache) or after the fetch cache expires; and the first production render after each deploy omits the section for ≤ 5 min. Verification waits for that window.
- [MEDIUM] F6 cost path: each OG image render = Next function → Python function (3 s bound) → Satori; 94 cold crawler fetches may hit cold starts. The generic-card fallback prevents 500s; image size unknown until built.
- [LOW] The sweep is ~140 Chrome runs across two passes (+2 extra builds for the state shots): mechanical, ~15–20 min per pass.
- [LOW] Brand slugs as page headings read lowercase (`adafruit`, `unexpected-maker`) — consistent with the site, weak as an H1 → Q9.
- [LOW] `gh pr merge --auto` is available (`allow_auto_merge: true`); if a PR lands while `main` moved, the auto-merge stalls on `strict` until I rebase — I will poll and rebase rather than assume.

## Questions for the author

1. **F7 Speed Insights** — Felipe never answered. Default **no**: the dependency is not added unless a yes arrives. Confirm.
2. **Chip chain on phones** — I recommend *shrink inline* (arrow + chip kept together; long name wraps inside its chip with the badge trailing the last word) as the default, and I will shoot the *vertical stack* variant too so Felipe picks from before/after pairs. OK?
3. **Sticky aside / picker** — recommend **always static** for both `.part-aside` and `.compare-picker`: pure-CSS sticky without `max-height` hides the bottom of a tall aside until the page ends, and "sticky only when it fits" needs JS (out of polish scope). Confirm.
4. **Compare CLS** — may I SSR the picker's part list (`fetchAllParts()` + `revalidate = 300` in `app/compare/page.tsx`, `initialParts` prop, client fetch only as fallback)? Keeps `noindex,follow` + `?ids=` client logic. If no: skeleton + reserved `min-height` only, CLS reported honestly.
5. **Screenshots** — commit ≤ 6 contact-sheet PNGs (≤ 200 KB each) under `docs/handovers/2026-08-22-seo-fixes-and-polish/assets/` (REVIEW-r1 answer 6 precedent) so before/after is visible on GitHub, or scratchpad paths only in the PR body?
6. **F4 placement** — "Browse by chip" as a full-width section after `<HomeView />` (default) or between the intro and the wizard?
7. **F4 SoC pages** — keep the aside "Related parts" (now duplicating the 34-item main list) or, on SoC pages only, replace the aside card body with one line linking to the hub section? Default: keep (least change).
8. **F5 matching** — exact `parts.vendor_or_brand = ?` as specified (slugs are lowercase; `?brand=Adafruit` → 0). Keep exact? (default yes; `form` uses `LOWER()` and could be mirrored instead).
9. **F5 heading/title** — slug verbatim (`adafruit — ESP32 boards and modules`) or a display map `brandLabel()` in `lib/format.ts` (presentation only, like `typeLabel`)? Default: verbatim.
10. **F5 home "Browse by brand"** — include in the F5 PR (REVIEW-r1: enters once F5 merges)? Default yes.
11. **F6 fonts** — `next/og` default font for r1, or fetch Newsreader / Geist Mono TTFs at render time (a network dependency per render)? Default: default font.
12. **F6 JSON-LD** — point `TechArticle.image` at the per-part image in the same PR? Default yes.
13. **`origin` semantics** — `browse` = home chip links + SoC hub list; `brand` = brand index/hub links + home brand links. OK?
