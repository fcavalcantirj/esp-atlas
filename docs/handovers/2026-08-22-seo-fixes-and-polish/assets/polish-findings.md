# Polish sweep — findings (before any CSS edit)

Method: one headless Chrome over CDP (`sweep.py`), `prefers-color-scheme` emulated (light + dark asserted via `data-theme`), viewports 390 / 768 / 1024 / 1440 × 900, full-page captures `shots/before/<slug>-<w>-<theme>.png`, layout facts in `measure-before.json`; chip-chain variants injected live (`chain-test.py`) at 320 / 360 / 390 / 1440.
Build: `main` 53ed50d, local `next start` on 3000 against the API on 8010.

| # | Route × width | Symptom (measured) | Cause — selector @ `apps/web/app/globals.css` line | Fix |
|---|---|---|---|---|
| 1 | board, soc-s3 @ 1024 / 1440 | `.part-aside` scrollHeight 1747 / 1729 vs clientHeight 818 → inner scrollbar; Related parts cut at "CoreS3" | `.part-aside { position: sticky; top; max-height: calc(100dvh - …); overflow-y: auto }` @ 1265–1270 | (a) delete the sticky/max-height/overflow declarations → static |
| 2 | compare-empty, compare-3 @ 1024 / 1440 | `.compare-picker` 846 vs 816 → inner scrollbar | `.compare-picker { position: sticky; … overflow-y: auto }` @ 1576–1581 | (a) same |
| 3 | compare-3 @ 390 | picker box 647 px wide in a 390 px viewport (`details.compare-picker right=647`, input / segmented / groups right=631); `html,body{overflow-x:hidden}` clips → filter, tabs, Clear and table columns unreachable | `.compare-layout { grid-template-columns: 1fr }` @ 1566 lets the table's min-content widen the column | (b) `minmax(0, 1fr)` + `.compare-layout > * { min-width: 0 }` |
| 4 | compare @ all widths | Suspense fallback `<p>Loading parts…</p>` → full layout after hydration; PSI CLS 0.591 / 0.278 | `app/compare/page.tsx:37` | (b) SSR picker list (`fetchAllParts()` + `revalidate = 300`, `initialParts`) + data-aware placeholder with the picker's real footprint |
| 5 | feather @ 320 / 360 (real phones); devkit chain | current chip wraps to 2 lines (h 52), kind badge vertically centred at the far right edge (badgeRight 334 of 345, 17 px from the top) instead of after the name | `.chip-chain-item { display: inline-flex; align-items: center; gap }` @ 1331; inner `span.chip-chain` (ChipChain.tsx:22) is a `flex-wrap: wrap` box | (c) shrink-inline: chip → `inline-block`, badge → `inline` (badge now trails the last word: badgeRight 60, 29 px from top); inner wrapper `inline-flex; nowrap` so arrow + chip are always one unit. Stranded arrow not reproducible at 320–1440 / scale 1 (the inner box is already atomic); nowrap makes it impossible at any text scale. Desktop delta: chip 2 px narrower (inline space vs 0.35rem gap) |
| 6 | all routes @ 390 | footer gutter 18.75 px vs header/main 15 px | `--page-x` defined only @ 37; the `@media (max-width: 599px)` block @ 1787 overrides `.container` and `.site-header-inner` with literal `1rem` but not `.site-footer-inner` @ 382 | (d) `:root { --page-x: 1rem }` in that block; overrides use `var(--page-x)` |
| 7 | board, soc-*, module, 404 @ 768 / 1024 / 1440 | `main.container` gutter 18.75 px vs header/footer 23.04 / 30.72 / 43.2 px — part pages and 404 don't share the page gutter the token comment promises ("shared by header, main and footer so everything aligns") | `.container { padding: 1.5rem 1.25rem 4rem }` @ 224–229 vs `.container--wide { padding-inline: var(--page-x) }` | (f, shown) `.container { padding-inline: var(--page-x); max-width: calc(1200px + 2 * var(--page-x)) }` — content column stays 1200 px, edges align with header/footer at ≤ 1286 px; at 1440 the centred column shifts 19 px left (content 1162 → 1200 px) |
| 8 | compare @ all widths | `.segmented` stretched to the picker width (328 / 690 / 308 px = parent) | `.compare-picker-body { display: flex; flex-direction: column }` @ 1619 stretches the inline-flex child | (e) `.compare-picker-body > .segmented { align-self: flex-start }` |
| 9 | part pages, compare @ all widths | Section labels "RADIOS / USB / SOURCES / CONTRIBUTE / RELATED PARTS / Boards on …" are serif uppercase at ad-hoc 0.78–0.9 rem, while home/footer labels are the restyle's mono overline (`.panel-title` @ 739, `.footer-title` @ 409 — "one label style site-wide") | `.spec-group h3` @ 1383, `.aside-card h2` @ 1455, `.related-group h3` @ 1516, `.compare-group > summary` @ 1656 | (f, shown) one shared overline rule (`--font-data`, `--t-micro`, 500, uppercase, `--track-label`, `--muted`) |
| 10 | part pages, compare @ all widths | Spec groups, aside cards and the compare picker are boxed cards (`background: var(--surface)` + border + radius) while the restyle states "no card chrome — plain sections separated by whitespace" (`.panel` @ 731, `.empty-state` @ 1195, `.results-list` @ 1027) | `.spec-group` @ 1376, `.aside-card` @ 1444, `.compare-picker` @ 1584 | (f, shown) flat: no fill, hairline top rule, same paddings → the restyle's panel idiom on every page |
| 11 | board @ 390 | breadcrumb wraps the part name onto its own line (`.breadcrumb` @ 1239, `flex-wrap: wrap`) | — | design read: leave (reads fine; name is the current crumb) |
| 12 | all routes | smallest text 10.2–10.3 px (`.badge` 0.68rem; `--t-micro` 0.6875rem on the 15 px root) | tokens | leave — type scale is Felipe's call (not in scope) |

Not found: horizontal overflow on `/`, part pages, 404 at any width; light/dark parity holds on all 64 shots; header wraps nav to a second row at 390 (intended).
States: `PartDetailClient` loading / error captured separately (`shots/states/`). `app/error.tsx` needs a thrown render — not reproducible without a code change, skipped.
