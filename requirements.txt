# Deps for the Vercel Python function (api/index.py). The local packages
# esp_atlas_api and esp_atlas_core are imported from bundled source (apps/*/src,
# shipped via vercel.json includeFiles) — NOT pip-installed: uv rejects path
# installs whose directory name differs from the package metadata name.
fastapi>=0.110
pyyaml>=6.0
httpx>=0.27
jsonschema>=4.0
groq>=0.9
