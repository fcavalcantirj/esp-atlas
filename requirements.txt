# Deps for the Vercel Python function at api/index.py (esp_atlas_api.main mounted
# under /api). Kept separate from apps/*/pyproject.toml, which govern local dev.
fastapi>=0.110
pyyaml>=6.0
httpx>=0.27
jsonschema>=4.0
groq>=0.9

./apps/core
./apps/api
